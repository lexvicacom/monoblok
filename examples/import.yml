import:
  servers:
    - "nats://127.0.0.1:14889"
  subject:
    - "raw.>"

export:
  servers:
    - "nats://127.0.0.1:14889"
  export:
    - "clean.>"

on:
  - sub: raw.temp
    thread:
      from: payload-float
      steps:
        - [round, 1]
        - [squelch]
        - [publish!, "clean.temp"]
