name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - c
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths-ignore:
      - '**/*.md'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x86_64
            runs-on: ubuntu-22.04
          - platform: macos-aarch64
            runs-on: macos-latest
    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure

  sanitizer:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Install sanitizer dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev
      - name: Configure ASan/UBSan
        run: cmake -S . -B build-asan -DCMAKE_BUILD_TYPE=Debug -DMB_ASAN=ON
      - name: Build ASan/UBSan
        run: cmake --build build-asan --parallel
      - name: Test ASan/UBSan
        run: ctest --test-dir build-asan --output-on-failure

  smoke:
    needs:
      - test
      - sanitizer
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Install smoke dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev netcat-openbsd
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Server smoke
        run: cmake --build build --target smoke --parallel
